{
  "name": "authorized-hls-vod-downloader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "copy:ffmpeg-core": "node scripts/copy-ffmpeg-core.mjs",
    "build": "npm run copy:ffmpeg-core && vite build",
    "dev": "vite build --watch"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.12.10",
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.307",
    "typescript": "^5.8.3",
    "vite": "^6.1.0"
  }
}
