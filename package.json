{
  "name": "pornhub-downloader",
  "version": "0.9.0-beta",
  "private": true,
  "type": "module",
  "scripts": {
    "copy:ffmpeg-core": "node scripts/copy-ffmpeg-core.mjs",
    "build": "npm run copy:ffmpeg-core && vite build --config vite.content.config.ts && vite build --config vite.offscreen.config.ts && vite build --config vite.background.config.ts",
    "dev": "npm run build -- --watch"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.12.10",
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.307",
    "typescript": "^5.8.3",
    "vite": "^6.1.0"
  }
}
